{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the foundation desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
